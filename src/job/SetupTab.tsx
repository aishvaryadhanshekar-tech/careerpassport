import { JobTabEmpty } from "../JobDetailsPage";

export function SetupTab() {
  return (
    <JobTabEmpty
      icon="⚙"
      title="Setup coming soon"
      blurb="Job configuration will live here — application form, pipeline stages and publishing settings."
    />
  );
}
